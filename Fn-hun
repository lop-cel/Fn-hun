local Players, RunService, UserInputService = game:GetService("Players"), game:GetService("RunService"), game:GetService("UserInputService") local player, camera = Players.LocalPlayer, workspace.CurrentCamera local playerGui = player:WaitForChild("PlayerGui")  -- Оптимизация графики для FPS local function optimizeGraphics() 	local lighting = game:GetService("Lighting") 	lighting.GlobalShadows = false 	lighting.FogEnd = 1e10 	lighting.Brightness = 1.5 	 	local workspace = game:GetService("Workspace") 	workspace.DescendantAdded:Connect(function(desc) 		if desc:IsA("BasePart") and not desc:IsA("Terrain") then 			desc.Material = Enum.Material.SmoothPlastic 			desc.Reflectance = 0 			if desc:IsA("MeshPart") or desc:IsA("Part") then 				desc.CastShadow = false 			end 		end 	end) 	 	for _, v in pairs(workspace:GetDescendants()) do 		if v:IsA("BasePart") and not v:IsA("Terrain") then 			v.Material = Enum.Material.SmoothPlastic 			v.Reflectance = 0 			if v:IsA("MeshPart") or v:IsA("Part") then 				v.CastShadow = false 			end 		end 	end end  -- Настройки качества local qualityLevel = 1 -- 1=минимальное, 2=среднее, 3=максимальное FPS local function setQuality(level) 	if level == 1 then -- Максимальный FPS 		settings().Rendering.QualityLevel = Enum.QualityLevel.Level01 		workspace.CurrentCamera.MaxAxisViewAngle = 50 	elseif level == 2 then -- Средний 		settings().Rendering.QualityLevel = Enum.QualityLevel.Level04 		workspace.CurrentCamera.MaxAxisViewAngle = 70 	else -- Высокое качество 		settings().Rendering.QualityLevel = Enum.QualityLevel.Level09 		workspace.CurrentCamera.MaxAxisViewAngle = 90 	end end  local gui = Instance.new("ScreenGui") gui.Name, gui.ResetOnSpawn, gui.Parent = "VlickGUI", false, playerGui  local mainFrame = Instance.new("Frame") mainFrame.Size, mainFrame.Position = UDim2.new(0, 280, 0, 400), UDim2.new(0.5, -140, 0.5, -200) mainFrame.BackgroundColor3, mainFrame.BackgroundTransparency = Color3.fromRGB(20, 20, 30), 0.1 mainFrame.BorderSizePixel, mainFrame.Active, mainFrame.Draggable = 0, true, true mainFrame.Parent = gui  Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 15)  local titleBar = Instance.new("Frame", mainFrame) titleBar.Size, titleBar.Position = UDim2.new(1, 0, 0, 35), UDim2.new(0, 0, 0, 0) titleBar.BackgroundColor3, titleBar.BorderSizePixel = Color3.fromRGB(40, 40, 55), 0 Instance.new("UICorner", titleBar).CornerRadius = UDim.new(0, 15)  local title = Instance.new("TextLabel", titleBar) title.Size, title.Position = UDim2.new(1, -40, 1, 0), UDim2.new(0, 15, 0, 0) title.BackgroundTransparency, title.Text = 1, "⚡ ВАЛХОП КОНТРОЛЬ" title.TextColor3, title.Font, title.TextSize, title.TextXAlignment = Color3.new(1,1,1), Enum.Font.GothamBold, 16, Enum.TextXAlignment.Left  local collapse = Instance.new("TextButton", titleBar) collapse.Size, collapse.Position = UDim2.new(0, 25, 0, 25), UDim2.new(1, -30, 0, 5) collapse.BackgroundColor3, collapse.Text, collapse.TextColor3 = Color3.fromRGB(60, 60, 80), "−", Color3.new(1,1,1) collapse.Font, collapse.TextSize = Enum.Font.GothamBold, 18 Instance.new("UICorner", collapse).CornerRadius = UDim.new(0, 8)  local fpsLabel = Instance.new("TextLabel", titleBar) fpsLabel.Size, fpsLabel.Position = UDim2.new(0, 60, 0, 20), UDim2.new(0.5, -30, 0, 7) fpsLabel.BackgroundColor3, fpsLabel.BackgroundTransparency = Color3.fromRGB(0,0,0), 0.5 fpsLabel.Text, fpsLabel.TextColor3 = "FPS: 60", Color3.new(0,1,0) fpsLabel.Font, fpsLabel.TextSize = Enum.Font.GothamBold, 12 Instance.new("UICorner", fpsLabel).CornerRadius = UDim.new(0, 5)  local content = Instance.new("Frame", mainFrame) content.Size, content.Position = UDim2.new(1, -20, 1, -45), UDim2.new(0, 10, 0, 40) content.BackgroundTransparency = 1  local function btn(txt, pos, col, size) 	local b = Instance.new("TextButton", content) 	b.Size = size or UDim2.new(0, 120, 0, 40) 	b.Position = pos 	b.BackgroundColor3 = col or Color3.fromRGB(50, 50, 70) 	b.Text, b.TextColor3 = txt, Color3.new(1,1,1) 	b.Font, b.TextSize = Enum.Font.GothamBold, 12 	Instance.new("UICorner", b).CornerRadius = UDim.new(0, 10) 	return b end  local function lbl(txt, pos, sz) 	local l = Instance.new("TextLabel", content) 	l.Size = sz or UDim2.new(0, 120, 0, 40) 	l.Position = pos 	l.BackgroundTransparency = 1 	l.Text, l.TextColor3 = txt, Color3.fromRGB(200, 200, 255) 	l.Font, l.TextSize = Enum.Font.Gotham, 12 	l.TextWrapped = true 	return l end  local manual = btn("РУЧНОЙ ВЛИК", UDim2.new(0,0,0,0)) local auto = btn("АВТО: ВЫКЛ", UDim2.new(0,140,0,0)) auto.TextColor3 = Color3.fromRGB(255,100,100)  local jumpFlick = btn("ВЛИК НА ПРЫЖОК: ВЫКЛ", UDim2.new(0,0,0,50), nil, UDim2.new(1,0,0,40)) jumpFlick.TextColor3 = Color3.fromRGB(255,100,100)  local speedLbl = lbl("СКОРОСТЬ: 0.05с", UDim2.new(0,0,0,100), UDim2.new(1,0,0,25))  local spUp = btn("+", UDim2.new(0.5,-45,0,130), Color3.fromRGB(70,130,200), UDim2.new(0,40,0,30)) spUp.TextSize = 18 local spDown = btn("-", UDim2.new(0.5,5,0,130), Color3.fromRGB(200,70,70), UDim2.new(0,40,0,30)) spDown.TextSize = 18  local modeBtn = btn("ГОРИЗОНТ", UDim2.new(0,0,0,170), Color3.fromRGB(80,80,120), UDim2.new(0,120,0,30)) modeBtn.TextSize = 11 local modeLbl = lbl("ВРАЩЕНИЕ\nГОРИЗОНТ", UDim2.new(0,140,0,170), UDim2.new(0,120,0,30))  local jumpBtn = btn("ВАЛХОП", UDim2.new(0,0,0,210), Color3.fromRGB(100,70,150)) jumpBtn.TextSize = 11 local jumpTimingLbl = lbl("ТАЙМИНГ\n50%", UDim2.new(0,140,0,210))  local aim = btn("ПРИЦЕЛ: ВЫКЛ", UDim2.new(0,0,0,250)) aim.TextColor3 = Color3.fromRGB(255,100,100)  local tUp = btn("+", UDim2.new(1,-70,0,250), Color3.fromRGB(70,130,200), UDim2.new(0,30,0,25)) tUp.TextSize = 16 local tDown = btn("-", UDim2.new(1,-35,0,250), Color3.fromRGB(200,70,70), UDim2.new(0,30,0,25)) tDown.TextSize = 16  -- Кнопки для FPS буста local fpsBoost = btn("FPS БУСТ: ВЫКЛ", UDim2.new(0,0,0,290), Color3.fromRGB(40,80,40), UDim2.new(0,120,0,30)) fpsBoost.TextColor3 = Color3.fromRGB(255,100,100) fpsBoost.TextSize = 11  local qualityBtn = btn("КАЧЕСТВО: MIN", UDim2.new(0,140,0,290), Color3.fromRGB(80,40,40), UDim2.new(0,120,0,30)) qualityBtn.TextColor3 = Color3.new(1,1,1) qualityBtn.TextSize = 11  local jumpStatus = lbl("⚪ ВАЛХОП: OFF", UDim2.new(0,0,0,330), UDim2.new(0.5,-5,0,25)) jumpStatus.TextColor3 = Color3.fromRGB(255,100,100) local fallStatus = lbl("🛡️ ЗАЩИТА: OFF", UDim2.new(0.5,5,0,330), UDim2.new(0.5,-5,0,25)) fallStatus.TextColor3 = Color3.fromRGB(255,100,100)  local fallBtn = btn("ЗАЩИТА: ВЫКЛ", UDim2.new(0.5,5,0,365), nil, UDim2.new(0.5,-5,0,30)) fallBtn.TextColor3 = Color3.fromRGB(255,100,100) fallBtn.TextSize = 11  local cross = Instance.new("Frame", gui) cross.Size, cross.Position, cross.BackgroundTransparency = UDim2.new(0,20,0,20), UDim2.new(0.5,-10,0.5,-10), 1 cross.Visible = false local function line(sz, pos, col) 	local l = Instance.new("Frame", cross) 	l.Size, l.Position, l.BackgroundColor3 = sz, pos, col 	l.BorderSizePixel = 0 end line(UDim2.new(1,0,0,2), UDim2.new(0,0,0.5,-1), Color3.new(0,1,0)) line(UDim2.new(0,2,1,0), UDim2.new(0.5,-1,0,0), Color3.new(0,1,0))  -- Variables local speed, jumpTiming = 0.05, 0.5 local isSpinning, autoEnabled, jumpEnabled, aimEnabled, noFall = false, false, false, false, false local jumpFlickEnabled = false local loopRun = false local rotationMode = "horizontal" local lastTime, frameCount = tick(), 0 local fps = 60 local fpsBoostEnabled = false  -- Оптимизация для низкого FPS local optimizationLevel = 1 local skipFrames = 0  local char = player.Character or player.CharacterAdded:Wait() local hum, root = char:WaitForChild("Humanoid"), char:WaitForChild("HumanoidRootPart")  -- Функция для выполнения влика local function flick() 	if isSpinning then return end 	isSpinning = true 	local start, orig = tick(), camera.CFrame 	local stepSkip = 0 	 	RunService:BindToRenderStep("Vlick", Enum.RenderPriority.Camera.Value + 1, function() 		if optimizationLevel > 1 then 			stepSkip = stepSkip + 1 			if stepSkip < optimizationLevel then return end 			stepSkip = 0 		end 		 		local elapsed = tick() - start 		local progress = elapsed / speed 		 		if elapsed >= speed then 			RunService:UnbindFromRenderStep("Vlick") 			camera.CFrame = orig 			isSpinning = false 			return 		end 		 		local angle = progress * math.rad(360) 		if rotationMode == "horizontal" then 			camera.CFrame = orig * CFrame.Angles(0, angle, 0) 		elseif rotationMode == "vertical" then 			camera.CFrame = orig * CFrame.Angles(angle, 0, 0) 		else 			camera.CFrame = orig * CFrame.Angles(angle * 0.5, angle, 0) 		end 	end) end  -- Функция для влика с прыжком (для режима "ВЛИК НА ПРЫЖОК") local function flickWithJump() 	if isSpinning then return end 	 	-- Выполняем прыжок 	if hum then 		hum.Jump = true 	end 	 	-- Выполняем влик 	flick() end  local function autoLoop() 	while autoEnabled and loopRun do  		flick()  		local waitTime = speed + (0.02 * optimizationLevel) 		task.wait(waitTime)  	end 	loopRun = false end  -- FPS счетчик и оптимизация RunService.Heartbeat:Connect(function() 	frameCount = frameCount + 1 	local currentTime = tick() 	if currentTime - lastTime >= 1 then 		fps = frameCount 		frameCount = 0 		lastTime = currentTime 		fpsLabel.Text = string.format("FPS: %d", fps) 		 		if fps < 30 then 			optimizationLevel = 3 			fpsLabel.TextColor3 = Color3.fromRGB(255,100,100) 		elseif fps < 45 then 			optimizationLevel = 2 			fpsLabel.TextColor3 = Color3.fromRGB(255,255,0) 		else 			optimizationLevel = 1 			fpsLabel.TextColor3 = Color3.new(0,1,0) 		end 	end end)  -- Влик на прыжок (выполняет 1 влик + 1 прыжок) UserInputService.JumpRequest:Connect(function() 	if jumpFlickEnabled and not isSpinning then 		flickWithJump() 	end end)  manual.MouseButton1Click:Connect(flick)  auto.MouseButton1Click:Connect(function() 	autoEnabled = not autoEnabled 	auto.Text, auto.TextColor3 = autoEnabled and "АВТО: ВКЛ" or "АВТО: ВЫКЛ", autoEnabled and Color3.new(0,1,0) or Color3.fromRGB(255,100,100) 	if autoEnabled and not loopRun then loopRun = true; task.spawn(autoLoop) else loopRun = false end end)  jumpFlick.MouseButton1Click:Connect(function() 	jumpFlickEnabled = not jumpFlickEnabled 	jumpFlick.Text, jumpFlick.TextColor3 = jumpFlickEnabled and "ВЛИК НА ПРЫЖОК: ВКЛ" or "ВЛИК НА ПРЫЖОК: ВЫКЛ", jumpFlickEnabled and Color3.new(0,1,0) or Color3.fromRGB(255,100,100) end)  modeBtn.MouseButton1Click:Connect(function() 	if rotationMode == "horizontal" then 		rotationMode = "vertical" 		modeBtn.Text = "ВЕРТИКАЛ" 		modeLbl.Text = "ВРАЩЕНИЕ\nВЕРТИКАЛ" 	elseif rotationMode == "vertical" then 		rotationMode = "both" 		modeBtn.Text = "ОБА" 		modeLbl.Text = "ВРАЩЕНИЕ\nГОР+ВЕРТ" 	else 		rotationMode = "horizontal" 		modeBtn.Text = "ГОРИЗОНТ" 		modeLbl.Text = "ВРАЩЕНИЕ\nГОРИЗОНТ" 	end end)  jumpBtn.MouseButton1Click:Connect(function() 	jumpEnabled = not jumpEnabled 	jumpBtn.BackgroundColor3 = jumpEnabled and Color3.fromRGB(150,100,200) or Color3.fromRGB(100,70,150) 	jumpStatus.Text, jumpStatus.TextColor3 = jumpEnabled and "🟢 ВАЛХОП: ON" or "⚪ ВАЛХОП: OFF", jumpEnabled and Color3.new(0,1,0) or Color3.fromRGB(255,100,100) end)  aim.MouseButton1Click:Connect(function() 	aimEnabled = not aimEnabled 	aim.Text, aim.TextColor3 = aimEnabled and "ПРИЦЕЛ: ВКЛ" or "ПРИЦЕЛ: ВЫКЛ", aimEnabled and Color3.new(0,1,0) or Color3.fromRGB(255,100,100) 	cross.Visible = aimEnabled end)  fpsBoost.MouseButton1Click:Connect(function() 	fpsBoostEnabled = not fpsBoostEnabled 	if fpsBoostEnabled then 		fpsBoost.Text, fpsBoost.TextColor3 = "FPS БУСТ: ВКЛ", Color3.new(0,1,0) 		optimizeGraphics() 		setQuality(1) 	else 		fpsBoost.Text, fpsBoost.TextColor3 = "FPS БУСТ: ВЫКЛ", Color3.fromRGB(255,100,100) 		setQuality(qualityLevel) 	end end)  qualityBtn.MouseButton1Click:Connect(function() 	qualityLevel = qualityLevel + 1 	if qualityLevel > 3 then qualityLevel = 1 end 	 	local qualities = {"MIN", "MED", "MAX"} 	local colors = {Color3.fromRGB(100,255,100), Color3.fromRGB(255,255,100), Color3.fromRGB(255,100,100)} 	qualityBtn.Text = "КАЧЕСТВО: " .. qualities[qualityLevel] 	qualityBtn.BackgroundColor3 = colors[qualityLevel] 	 	if not fpsBoostEnabled then 		setQuality(qualityLevel) 	end end)  fallBtn.MouseButton1Click:Connect(function() 	noFall = not noFall 	fallBtn.Text, fallBtn.TextColor3 = noFall and "ЗАЩИТА: ВКЛ" or "ЗАЩИТА: ВЫКЛ", noFall and Color3.new(0,1,0) or Color3.fromRGB(255,100,100) 	fallStatus.Text, fallStatus.TextColor3 = noFall and "🛡️ ЗАЩИТА: ON" or "🛡️ ЗАЩИТА: OFF", noFall and Color3.new(0,1,0) or Color3.fromRGB(255,100,100) end)  spUp.MouseButton1Click:Connect(function() speed = math.min(speed + 0.01, 0.2); speedLbl.Text = string.format("СКОРОСТЬ: %.2fс", speed) end) spDown.MouseButton1Click:Connect(function() speed = math.max(speed - 0.01, 0.02); speedLbl.Text = string.format("СКОРОСТЬ: %.2fс", speed) end) tUp.MouseButton1Click:Connect(function() jumpTiming = math.min(jumpTiming + 0.05, 0.95); jumpTimingLbl.Text = string.format("ТАЙМИНГ\n%d%%", jumpTiming*100) end) tDown.MouseButton1Click:Connect(function() jumpTiming = math.max(jumpTiming - 0.05, 0.05); jumpTimingLbl.Text = string.format("ТАЙМИНГ\n%d%%", jumpTiming*100) end)  local collapsed = false collapse.MouseButton1Click:Connect(function() 	collapsed = not collapsed 	mainFrame.Size = collapsed and UDim2.new(0,280,0,40) or UDim2.new(0,280,0,400) 	collapse.Text = collapsed and "+" or "−" 	content.Visible = not collapsed end)  RunService.Heartbeat:Connect(function() 	if noFall and hum and hum:GetState() == Enum.HumanoidStateType.Freefall then hum.Jump = true end end)  -- Автоматическая оптимизация при запуске optimizeGraphics() setQuality(1)
